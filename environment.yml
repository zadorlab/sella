name: sella
channels:
  - defaults
  - conda-forge
dependencies:
  - python >= 3.9
  - pip
  - numpy >= 1.14.0
  - scipy >= 1.1.0
  - ase >= 3.22.0
  - jax >= 0.4.20
  - jaxlib >= 0.4.20
  - setuptools >= 74.1.0, < 82
  - setuptools-scm >= 8.0
